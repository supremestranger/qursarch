services:
  backend:
    build: backend
    volumes:
      - ./backend/:/src
  nginx:
    image: nginx:latest
    volumes:
      - type: bind
        source: ./nginx/nginx.conf
        target: /etc/nginx/conf.d/default.conf
    ports:
      - "3000:3000"
    depends_on:
      - backend